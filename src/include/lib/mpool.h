#include "../../include/pipenet.h"
template <unsigned int Ksize, unsigned int InputChannelNum>
void mpool(vT[InputChannelNum] input_pixel, vT[InputChannelNum] output_pixel) {
  INFO << "Implement max pooling in Line/Window Buffer is the same idea with "
          "the Convolution case."
       << endl;
}
